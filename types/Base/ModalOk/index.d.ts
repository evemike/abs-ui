/***
 * 模态框类型一自定义，计划用于下列类型弹窗
 * --无数据的状态历史（历史记录不足，请稍后重试）确定
 * --重放上一个事件（主节点、所有节点）
 */
import { App } from 'vue';
declare const _default: {
    install: (app: App) => void;
};
export default _default;
