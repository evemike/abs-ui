export declare namespace IElsMarkdown {
    interface Props {
        modelValue: string;
        options?: Record<string, any>;
    }
    interface ViewProps {
        value: string;
        theme?: Record<string, any>;
    }
}
