export declare namespace IElsMarkdown {
    interface Props {
        modelValue: string;
        options?: Record<string, any>;
    }
    interface ViewProps {
        value: string;
        options?: Record<string, any>;
    }
}
